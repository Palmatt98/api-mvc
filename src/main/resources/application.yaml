#spring.application.name=API-MVC
#debug=true

spring:
  application:
    name: API-MVC
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/concessionaria
    username: root
    password: root